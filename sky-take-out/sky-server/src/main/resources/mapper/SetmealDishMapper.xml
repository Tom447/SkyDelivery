<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <delete id="deleteBySetmealIds">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <resultMap id="DishWithSetmealDishMap" type="com.sky.vo.SetmealDishVO">
        <result property="copies" column="copies"/>
        <result property="image" column="image"/>
        <result property="description" column="description"/>
        <result property="name" column="name"/>
    </resultMap>

    <select id="getDishBySetmealId" resultMap="DishWithSetmealDishMap">
        SELECT
        sd.copies     AS copies,
        d.image       AS image,
        d.description AS description,
        d.name        AS name
        FROM setmeal_dish sd
        LEFT JOIN dish d ON sd.dish_id = d.id
        WHERE d.status = 1
        <if test="setmealId != null">
            AND sd.setmeal_id = #{setmealId}
        </if>
        ORDER BY d.create_time DESC
    </select>

    <select id="getSetmealIdsByDishId" resultType="java.lang.Long">
        SELECT sd.setmeal_id
        FROM setmeal_dish sd
        <where>
            dish_id In
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
        </where>
    </select>


</mapper>
