<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrdersDetailMapper">


    <insert id="insertBatch">
        INSERT INTO order_detail (
        id,
        order_id,
        dish_id,
        setmeal_id,
        name,
        image,
        dish_flavor,
        number,
        amount
        ) VALUES
        <foreach collection="orderDetailList" item="detail" separator=",">
            (
            #{detail.id},
            #{detail.orderId},
            #{detail.dishId},
            #{detail.setmealId},
            #{detail.name},
            #{detail.image},
            #{detail.dishFlavor},
            #{detail.number},
            #{detail.amount}
            )
        </foreach>
    </insert>
    <select id="getOrderDetailsByOrderIds" resultType="com.sky.entity.OrderDetail">
        SELECT * FROM order_detail
        WHERE order_id IN
        <foreach collection="orderIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
