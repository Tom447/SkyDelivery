spring:
  profiles:
    active: dev #配置环境信息
  main:
    allow-circular-references: true #配置自动解决循环依赖
  datasource: #配置数据库连接信息
    druid:
      driver-class-name: ${sky.datasource.driver-class-name}
      url: jdbc:mysql://${sky.datasource.host}:${sky.datasource.port}/${sky.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${sky.datasource.username}
      password: ${sky.datasource.password}
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher #设置SpringMVC的路径匹配方式与Swagger的路径匹配方式一致.
  #  配置redis数据库的信息
  redis:
    host: ${sky.redis.host}
    port: ${sky.redis.port}
    database: ${sky.redis.database}
    password: ${sky.redis.password}
  #  配置电子邮件信息
  mail:
    host: smtp.qq.com
    port: 465
    password: szlwjdtvhockhhii
    username: 1142322503@qq.com
    protocol: smtps



#Mybatis的配置信息
mybatis:
  mapper-locations: classpath:mapper/*.xml  #mapper配置文件
  configuration:
    map-underscore-to-camel-case: true  #开启驼峰命名

#配置日志级别
logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info

#配置JWT的信息
sky:
  jwt:
    admin-secret-key: itcast #设置jwt签名加密时使用的秘钥
    admin-ttl: 604800000 #设置jwt过期时间
    admin-token-name: token #设置前端传递过来的令牌名称
    user-secret-key: itheima #设置C端jwt签名加密时使用的秘钥
    user-ttl: 604800000 #设置C端jwt的过期时间
    user-token-name: authentication

  alioss: #阿里云oss存储文件
    endpoint: ${OSS_ENDPOINT:oss-cn-beijing.aliyuncs.com}
    accessKeyId: ${OSS_ACCESS_KEY_ID:}
    accessKeySecret: ${OSS_ACCESS_KEY_SECRET:}
    bucketName: ${OSS_BUCKET_NAME:mawangde01-20250929-204402}

  wechat: #微信小程序的appid
    appid: ${sky.wechat.appid}
    secret: ${sky.wechat.secret}
    #以下是支付相关配置信息
    mchid: ${sky.wechat.mchid}
    mchSerialNo: ${sky.wechat.mchSerialNo}
    privateKeyFilePath: ${sky.wechat.privateKeyFilePath}
    apiV3Key: ${sky.wechat.apiV3Key}
    weChatPayCertFilePath: ${sky.wechat.weChatPayCertFilePath}
    notifyUrl: ${sky.wechat.notifyUrl}
    refundNotifyUrl: ${sky.wechat.refundNotifyUrl}
    # 新增：是否启用沙箱
    sandbox: true

#端口号
server:
  port: 8080
